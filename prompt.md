# Original Development Prompt

Yo, it's me I want to build a To-Do List app for my shopback take home assignment the minimum features required for this assignment are:

1. Add, edit, delete tasks
2. Mark tasks as complete
3. Basic persistence (e.g. localStorage, file, DB)

Tech Stack i'm comfortable with debugging and changing code with is MERN (MongoDB, Express, Node.js, React):
I think it's fine to include the API keys in the submission as the keys will be specifically generated for this particular project with restrictions for others to use.

This is my plan for the development and the route we are going to take:

1. Start by creating a list of a features to be included in the app that are implementable by you within 2 hours. The features should be user friendly and design should look and feel premium while using instead of just AI junk.
2. Create wireframes for the features described with a me in the loop tweaking them around.
3. Create the plan of backend endpoints we'll need this for this app.
4. We'll do a bottom up approach while building this app. so the endpoints and features that don't rely on others go first in development.
5. After each build we're going to do unit tests and integration tests for the app so be ready for that too.

These are the files Im supposed to submit:

1. codebase: your complete, working applicatoin source files
2. README.md: instructions on how to run the app
3. promt.md: This prompt
4. Screenshots of the UI for the app.
5. reflection.md: An AI usage write up (max 500 words)

We'll be updating the reflection.md and codebase along the development process so create a skill to work on that while development is still ongoing.

Here's how we are going to start:
You have to research the internet for good todo lists apps that are doable by just AI generation in a couple of hours. select features that can be implemented and create the requirements file for now. Also setup the project folder that is attached to this codex project. Write an AGENT.md file that can be used for token optimisation too.

